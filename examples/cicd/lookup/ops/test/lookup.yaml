cassandra:
  replication:
    strategy: org.apache.cassandra.locator.SimpleStrategy
    factor: 1
  consistency:
    read: ONE
    write: ONE
  security:
    ssl_enabled: false

db:
  metrics:
    url: "jdbc:mysql://127.0.0.1:3306/metrics"
    user: "metrics"
    password: "changeme"
