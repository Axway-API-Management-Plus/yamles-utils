# All values for which Ops is responsible for
Server_Settings:
  Cassandra_Settings:
    username: "{{ _ops_kdb('/prod/ops-keystore', 'user') }}"
    password: "{{ _ops_kdb('/prod/ops-keystore', 'password') | base64encode }}"
